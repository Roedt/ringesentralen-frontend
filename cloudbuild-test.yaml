steps:
  # Create docker image
  - name: gcr.io/cloud-builders/docker
    entrypoint: "bash"
    args:
      - "-c"
      - |
        docker build  --build-arg=HYPERSYSURL=$_HYPERSYSURL --build-arg=SENTRYURL=$_SENTRYURL --build-arg=IS_PRODUCTION=$_IS_PRODUCTION --build-arg=SYSTEMBRUKER=$$SYSTEMBRUKER --build-arg=SYSTEMBRUKERPASSORD=$$SYSTEMBRUKERPASSORD --build-arg=FRONTENDTOKENKEY=$$FRONTENDTOKENKEY -t gcr.io/$PROJECT_ID/ringesentralen-frontend .
    secretEnv: ['SYSTEMBRUKER', 'SYSTEMBRUKERPASSORD', 'FRONTENDTOKENKEY']
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/ringesentralen-frontend']

  # Start up a cloud instance
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      [
      'beta',
      'run',
      'deploy',
      'ringesentralen-frontend',
      '--image', 'gcr.io/$PROJECT_ID/ringesentralen-frontend',
      '--allow-unauthenticated',
      '--platform=managed',
      '--region=europe-north1'
      ]

# Image name
images: ['gcr.io/$PROJECT_ID/ringesentralen-frontend']
availableSecrets:
  secretManager:
  - versionName: projects/$PROJECT_ID/secrets/frontendTokenKey/versions/1
    env: 'FRONTENDTOKENKEY'
  - versionName: projects/$PROJECT_ID/secrets/frontendSystembruker/versions/1
    env: 'SYSTEMBRUKER'
  - versionName: projects/$PROJECT_ID/secrets/frontendSystembrukerPassord/versions/1
    env: 'SYSTEMBRUKERPASSORD'